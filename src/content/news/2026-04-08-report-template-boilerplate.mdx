export const metadata = {
	focus: 'reportTemplate',
	title: 'Boilerplate: Report-Template für neue Ratings',
	summary: 'Dieses Template zeigt alle relevanten Markdown-Elemente und kann direkt als Startpunkt für neue Report-MDs verwendet werden.',
};

import ReportTemplate from '../reports/report-template.md';

# Report-Template als erster Referenz-Post

Dieser Beitrag rendert das komplette Report-Template direkt in der App.

<ReportTemplate />
