{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Relation Definition",
	"description": "Defines a relationship or compatibility between two items in the digital stack",
	"type": "object",
	"required": ["from", "to", "types"],
	"additionalProperties": false,
	"properties": {
		"from": {
			"type": "string",
			"description": "Source item ID (must exist in data/items/)"
		},
		"to": {
			"type": "string",
			"description": "Target item ID (must exist in data/items/)"
		},
		"types": {
			"type": "array",
			"description": "Types of relationships between the items",
			"items": {
				"type": "string",
				"enum": ["requires", "complements", "replaces"]
			},
			"minItems": 1
		},
		"score": {
			"type": "number",
			"description": "Compatibility/strength score (0–1)",
			"minimum": 0,
			"maximum": 1
		},
		"note": {
			"type": "object",
			"description": "Optional localized explanation of the relationship",
			"additionalProperties": false,
			"properties": {
				"de": { "type": "string" },
				"en": { "type": "string" },
				"fr": { "type": "string" }
			}
		}
	}
}
